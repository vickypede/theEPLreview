# July 2026 Season Update Plan

## Confirmed 2026/27 Premier League Clubs

- Arsenal
- Aston Villa
- Bournemouth
- Brentford
- Brighton & Hove Albion
- Chelsea
- Coventry City
- Crystal Palace
- Everton
- Fulham
- Hull City
- Ipswich Town
- Leeds United
- Liverpool
- Manchester City
- Manchester United
- Newcastle United
- Nottingham Forest
- Sunderland
- Tottenham Hotspur

## Slug Set

- `arsenal`
- `aston-villa`
- `bournemouth`
- `brentford`
- `brighton`
- `chelsea`
- `coventry`
- `crystal-palace`
- `everton`
- `fulham`
- `hull-city`
- `ipswich`
- `leeds`
- `liverpool`
- `manchester-city`
- `manchester-united`
- `newcastle`
- `nottingham-forest`
- `sunderland`
- `tottenham`

## Clubs To Hide Or Mark Inactive

These should no longer appear as current Premier League clubs:

- `burnley`
- `west-ham`
- `wolves`

Do not delete them immediately unless we decide old club pages/articles should disappear. Prefer marking them inactive for the current Premier League season.

## Data Model Recommendation

Make `isCurrentPremierLeague` the source of truth for active Premier League membership.

Every club document should carry:

```json
{
  "isCurrentPremierLeague": true,
  "season": "2026-27"
}
```

If `isCurrentPremierLeague` is not `true`, the club should be treated as inactive for the current EPL experience.

Use `isCurrentPremierLeague` to decide what appears in:

- `/clubs`
- Header club dropdown
- Homepage club fallback selection
- Profile club selector
- Admin source club selector
- Any future sitemap club entries
- Club detector/tagging during new article ingestion
- Whether club-specific sources should run

Use `isTop6` only for:

- Priority ingestion grouping
- Homepage top-six club cards
- Default followed clubs for new users

Keep `isTop6` for deliberate editorial priority. It should not be used as the source of truth for current Premier League membership.

## Priority / Top Six Logic

Preserve the existing deliberate behavior:

- `group=top6` runs general sources plus sources tagged to current clubs where `isTop6` is `true`.
- `group=other14` runs general sources plus sources tagged to current clubs where `isTop6` is `false`.
- General sources with `clubSlugs: []` run in both grouped jobs.
- Sources tagged only to inactive clubs should not run.

Current top-six should remain:

- `arsenal`
- `chelsea`
- `liverpool`
- `manchester-city`
- `manchester-united`
- `tottenham`

Do not mark promoted clubs as `isTop6` unless the editorial strategy changes.

## Seed Data Work

Unify the club seed files. Current repo state has drift:

- `functions/src/seed/clubs.json` has 19 clubs and is missing some current clubs.
- `functions/scripts/clubs.json` has stale clubs like `luton` and `sheffield-united`.

Make one canonical club source and ensure seeding reads from it.

The seed function must write `isCurrentPremierLeague` into Firestore. Adding the field to JSON is not enough if the seed function ignores it.

Recommended seed fields:

- `id`
- `name`
- `isTop6`
- `isCurrentPremierLeague`
- `season`
- `names`
- `ambiguous`
- `badgeUrl`

Add aliases for promoted clubs:

- Coventry City: `Coventry`, `Coventry City`, `Sky Blues`, `CCFC`
- Hull City: `Hull`, `Hull City`, `Hull City AFC`, `Tigers`, `HCFC`
- Ipswich Town: `Ipswich`, `Ipswich Town`, `Tractor Boys`, `ITFC`
- Sunderland: `Sunderland`, `Sunderland AFC`, `Black Cats`, `SAFC`

## ScoreAxis Work

League widgets in `Stats.tsx` use a Premier League token and may roll forward automatically.

Team pages in `TeamPanel.tsx` use hardcoded team-info tokens. Verify and add tokens for:

- `coventry`
- `hull-city`
- `ipswich`

Existing tokens should also be reviewed for:

- `leeds`
- `sunderland`

Remove or leave inactive old tokens for:

- `burnley`
- `west-ham`
- `wolves`

## Ingestion Work

Update `functions/src/seed/sources.json`.

Add or verify club-specific sources for:

- Coventry City
- Hull City
- Ipswich Town
- Sunderland

Decide whether old club-specific sources should be disabled:

- Burnley
- West Ham
- Wolves

Recommended behavior:

- Keep general football/Premier League sources active.
- Keep top-six sources frequent.
- Keep non-top-six current club sources less frequent.
- Do not run sources tagged only to clubs where `isCurrentPremierLeague` is not `true`.
- Do not tag newly ingested articles with inactive clubs during automatic club detection.
- Do not ingest relegated-club-only sources into the active EPL experience unless intentionally keeping wider English football coverage.

Ingestion rules:

```text
If source.clubSlugs is empty:
  Run the source.
  Detect and tag current Premier League clubs only.

If source.clubSlugs has clubs:
  Run the source only if at least one clubSlug belongs to a club where isCurrentPremierLeague is true.

If group=top6:
  Run general sources plus current clubs where isTop6 is true.

If group=other14:
  Run general sources plus current clubs where isTop6 is false.
```

## Firestore Migration

A Vercel deploy is not enough because the live club list comes from Firestore.

After code/data changes:

1. Deploy app changes through Git/Vercel.
2. Deploy any Functions changes if needed.
3. Run a Firestore migration or seed to upsert current clubs.
4. Mark relegated clubs `isCurrentPremierLeague: false`.
5. Verify `/clubs`, header dropdown, profile selector, homepage club cards, and team pages.

## Verification Checklist

- `/clubs` shows exactly 20 current clubs.
- Header dropdown shows exactly 20 current clubs.
- Homepage still shows the deliberate top-six club cards.
- `/clubs/coventry`, `/clubs/hull-city`, `/clubs/ipswich`, and `/clubs/sunderland` render without "Team Not Found".
- ScoreAxis widgets render for promoted clubs or show an intentional fallback.
- `/table` renders the current Premier League table/top players.
- New user profile defaults still follow top six only.
- Admin source selector only shows current clubs unless archive mode is intentionally added.
- Ingestion run with `group=top6` still prioritizes top-six sources.
- Ingestion run with `group=other14` covers the remaining current Premier League clubs.
